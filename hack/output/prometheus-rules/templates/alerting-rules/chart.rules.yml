---
# Source: prometheus-rules/templates/alerting-rules/chart.rules.yml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: "prometheus-rules"
    app.kubernetes.io/instance: "RELEASE-NAME"
    app.giantswarm.io/branch: "[[ -Branch ]]"
    app.giantswarm.io/commit: "[[ .SHA ]]"
    app.kubernetes.io/managed-by: "Helm"
    app.kubernetes.io/version: "[[ .AppVersion ]]"
    helm.sh/chart: "prometheus-rules-2.1.1"
    giantswarm.io/service-type: managed
  name: chart.rules
  namespace: monitoring
spec:
  groups:
  - name: chart
    rules:
    - alert: ChartOrphanConfigMap
      annotations:
        description: 'Chart configmaps have not been deleted.'
        opsrecipe: chart-orphan-resources/
      expr: chart_operator_configmap_orphan > 0
      for: 10m
      labels:
        area: managedservices
        cancel_if_outside_working_hours: "false"
        severity: notify
        team: honeybadger
        topic: releng
