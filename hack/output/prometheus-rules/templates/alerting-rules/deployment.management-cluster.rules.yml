---
# Source: prometheus-rules/templates/alerting-rules/deployment.management-cluster.rules.yml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: "prometheus-rules"
    app.kubernetes.io/instance: "RELEASE-NAME"
    app.giantswarm.io/branch: "[[ -Branch ]]"
    app.giantswarm.io/commit: "[[ .SHA ]]"
    app.kubernetes.io/managed-by: "Helm"
    app.kubernetes.io/version: "[[ .AppVersion ]]"
    helm.sh/chart: "prometheus-rules-2.1.1"
    giantswarm.io/service-type: managed
    cluster_type: "management_cluster"
  name: deployment.management-cluster.rules
  namespace: monitoring
spec:
  groups:
  - name: deployment
    rules:
    - alert: DeploymentNotSatisfiedAtlas
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"alertmanager.*|grafana.*|prometheus.*|promxy.*", cluster_id!~"argali|giraffe"} > 0
      for: 30m
      labels:
        area: kaas
        cancel_if_cluster_status_creating: "true"
        cancel_if_cluster_status_deleting: "true"
        cancel_if_cluster_status_updating: "true"
        cancel_if_outside_working_hours: "false"
        severity: page
        team: atlas
        topic: managementcluster
    - alert: DeploymentNotSatisfiedChinaAtlas
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"alertmanager.*|grafana.*|prometheus.*|promxy.*", cluster_id=~"argali|giraffe"} > 0
      for: 30m
      labels:
        area: kaas
        cancel_if_cluster_status_creating: "true"
        cancel_if_cluster_status_deleting: "true"
        cancel_if_cluster_status_updating: "true"
        cancel_if_outside_working_hours: "false"
        severity: page
        team: atlas
        topic: managementcluster
    - alert: DeploymentNotSatisfiedHoneybadger
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"app-admission-controller-.+|app-operator-.+|chart-operator-.+|kyverno.*", cluster_id!~"argali|giraffe"} > 0
      for: 30m
      labels:
        area: managedservices
        cancel_if_cluster_status_creating: "true"
        cancel_if_cluster_status_deleting: "true"
        cancel_if_cluster_status_updating: "true"
        cancel_if_outside_working_hours: "false"
        severity: page
        team: honeybadger
        topic: managementcluster
    - alert: DeploymentNotSatisfiedChinaHoneybadger
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied-china/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"app-admission-controller-.+|app-operator-.+|chart-operator-.+|kyverno.*", cluster_id=~"argali|giraffe"} > 0
      for: 3h
      labels:
        area: managedservices
        severity: page
        team: honeybadger
        topic: managementcluster
    - alert: DeploymentNotSatisfiedKaas
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"aws-admission-controller.*|aws-operator-.+|azure-admission-controller-.+|azure-operator.*|azure-collector.*|cluster-operator-.+|cluster-api-core-webhook.*|coredns-.+|event-exporter-.*|upgrade-schedule-operator.*|nginx-ingress-controller-.+|worker-.+|master-.+", cluster_id!~"argali|giraffe"} > 0
      for: 30m
      labels:
        area: kaas
        cancel_if_cluster_status_creating: "true"
        cancel_if_cluster_status_deleting: "true"
        cancel_if_cluster_status_updating: "true"
        severity: page
        team: phoenix
        topic: managementcluster
    - alert: DeploymentNotSatisfiedChinaKaas
      annotations:
        description: 'Deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.'
        opsrecipe: deployment-not-satisfied-china/
      expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", deployment=~"aws-operator-.+|cluster-operator-.+|coredns-.+|event-exporter-.+|nginx-ingress-controller.*", cluster_id=~"argali|giraffe"} > 0
      for: 3h
      labels:
        area: kaas
        severity: page
        team: phoenix
        topic: managementcluster
