apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  creationTimestamp: null
  labels:
    {{- include "labels.common" . | nindent 4 }}
  name: konfigure-operator.rules
  namespace: {{ .Values.namespace  }}
spec:
  groups:
    - name: konfigure-operator
      rules:
        - alert: KonfigureOperatorDeploymentNotSatisfied
          annotations:
            description: '{{`Konfigure Operator deployment {{ $labels.namespace}}/{{ $labels.deployment }} is not satisfied.`}}'
            runbook_url: '{{`https://intranet.giantswarm.io/docs/support-and-ops/runbooks/deployment-not-satisfied/?INSTALLATION={{ $labels.installation }}&CLUSTER={{ $labels.cluster_id }}&NAMESPACE={{ $labels.namespace }}&KIND=deployment&NAME={{ $labels.deployment }}`}}'
          expr: kube_deployment_status_replicas_unavailable{cluster_type="management_cluster", namespace="giantswarm", deployment="konfigure-operator"} > 0
          for: 30m
          labels:
            area: platform
            cancel_if_outside_working_hours: "true"
            severity: page
            team: honeybadger
            topic: managementcluster
        - alert: KonfigureOperatorSchemaFetchFailed
          annotations:
            description: |-
              This is temporary alert meant to test the theory of possible non-200 responses causing
              configuration wipe-out. See the https://github.com/giantswarm/giantswarm/issues/36392
          expr: konfigure_operator_schema_fetch_total{status_code!~"200|0"} > 0
          labels:
            area: platform
            cancel_if_outside_working_hours: "true"
            severity: page
            team: honeybadger
            topic: managementcluster
        - alert: KonfigureOperatorSchemaFetchFailedAreaOnCall
          annotations:
            description: |-
              This is temporary alert meant to test the theory of possible non-200 responses causing
              configuration wipe-out. See the https://github.com/giantswarm/giantswarm/issues/36392
          expr: konfigure_operator_schema_fetch_total{status_code!~"200|0"} > 0
          labels:
            area: platform
            severity: notify
            team: honeybadger
            topic: managementcluster
        - alert: KonfigurationReconciliationFailed
          annotations:
            description: |-
              {{`{{ $labels.resource_kind }} {{ $labels.resource_name }} in ns {{ $labels.resource_namespace }} on {{ $labels.installation }} is stuck in Failed state.`}}
            runbook_url: https://intranet.giantswarm.io/docs/support-and-ops/runbooks/konfigure-operator/
          expr: konfigure_operator_reconcile_condition{condition_type="Ready", condition_status="False"} > 0
          for: 10m
          labels:
            area: platform
            cancel_if_outside_working_hours: "true"
            severity: page
            team: honeybadger
            topic: releng
            namespace: |-
              {{`{{ $labels.exported_namespace }}`}}
